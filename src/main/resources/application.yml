spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userService
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: root
      password: root
server:
  port: 8081